<%= form_with(model: [user, order], local: true) do |form| %>
  <% if order.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order.errors.count, "error") %> prohibited this order from being saved:</h2>

      <ul>
      <% order.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
<p>Start by naming your order and selecting your delivery date and time. </p>
  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :order_name %>
  </div>

  <div class="field">
    <%= form.label :'delivery Date' %>
    <%= form.datetime_select :date, id: :order_date %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
