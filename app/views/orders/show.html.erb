<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @order.name %>
</p>

<p>
  <strong>Date:</strong>
  <%= @order.date %>
</p>

<% if logged_in? %>
    <%= link_to 'Add Foods', "users/#{current_user.id}/orders/#{current_order}/add_food", method: :post %>
<% end %> |
<%= link_to 'Back', user_orders_path %>
